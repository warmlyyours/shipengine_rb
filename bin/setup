#!/usr/bin/env bash
set -euo pipefail
IFS=$'\n\t'
set -vx

bundle install

if command -v overcommit &> /dev/null; then
  overcommit --install
else
  echo "Skipping overcommit (not installed). Run: gem install overcommit"
fi
